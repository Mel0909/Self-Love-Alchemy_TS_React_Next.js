info:
  name: CriarCategoria
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/api/categoria
  body:
    type: json
    data: |-
      {
        "nome": "Velas Místicas"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
